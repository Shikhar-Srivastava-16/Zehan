use std::vec;

// use Zehan::tools::array_tools::help;
// use Zehan::utils::array_tools;

use Zehan::tensor_utils;

fn main() {
    let v1: Vec<i128> = vec![1, 2, 3];
    let v2: Vec<i128> = vec![1, 2, 3];
}
